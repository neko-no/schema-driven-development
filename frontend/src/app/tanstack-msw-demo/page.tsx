import SampleClient from "@/components/SampleClient";
import SampleServer from "@/components/SampleServer";

const Page = () => {
  return (
    <>
      <SampleServer />
      <SampleClient />
    </>
  );
};

export default Page;
